rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Hanya mengizinkan akses untuk pengguna yang sudah login
      allow read, write: if request.auth != null;
    }
  }
}
