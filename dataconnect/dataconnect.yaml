specVersion: "v1"
serviceId: "vitaringapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vitaringapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
